// Keep terminal rows flush with block and Powerline separator glyphs. Values
// above 1 add vertical space around the font while the glyphs remain at their
// original height, leaving visible gaps between prompt segments.
export const DEFAULT_TERMINAL_LINE_HEIGHT = 1
